---
name: rwang-core
description: RWANG:Core — six standing behavior rules (R1–R6) that stay in effect for every task once loaded. Use when the user says "RWANG:Core", invokes /rwang-core, or asks to load/enforce the RWANG core rules, coding discipline, complexity-based execution (C-1/C-2/C-3), or RCA-first debugging. Part of the RWANG: command family.
---

# RWANG:Core

1. **The SSOT is `RWANG-CORE.md` in this skill's directory** — read it in full. Never copy it into the project (legacy exception: a project's own copy wins there).
2. From this point on, rules R1–R6 are **always in effect** for every task in this session — they are not per-invocation.
3. RCA documents (per R5) go to the project's `docs/rca/` using this skill's `templates/RCA.md`.
4. Confirm activation in one line, and if a task is pending, state its C-1/C-2/C-3 classification and risk level per R4.
